# 22.md — 重构进度记录（对照 `docs/11.md`）

更新时间：2026-05-01  
记录范围：`src/`、`src-tauri/src/`、`tests/`

## 1. 执行口径

- 强制规则：新功能逻辑优先；旧逻辑若与新逻辑冲突，必须重构收敛到新逻辑。
- 本阶段目标：`docs/superpowers/plans/2026-05-01-refactor-realignment-phase1.md` 的 Phase 1。
- 验证原则：优先运行与改动直接相关的测试与 `typecheck`，无法执行时明确记录原因。

## 2. Phase 1 完成进度

### 已完成（Task 1：后端持久化策略与冻结区守卫模块化）

1. 从 `orchestrator.rs` 抽离持久化策略：
   - 新增 [`persist_policy.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/persist_policy.rs)
   - 抽离能力：`PersistMode`、`parse_persist_mode`、`infer_legacy_persist_mode`、`should_persist_task_output`、`is_derived_review_task`
2. 抽离冻结区冲突守卫：
   - 新增 [`freeze_guard.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/freeze_guard.rs)
   - 抽离能力：`detect_freeze_conflict`、`freeze_conflict_error`
3. 编排接线：
   - 修改 [`mod.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/mod.rs)
   - 修改 [`orchestrator.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/orchestrator.rs)

### 已完成（Task 2：运行态状态回写策略抽离）

1. 新增运行态状态回写策略模块：
   - 新增 [`runtime_state_writer.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/runtime_state_writer.rs)
   - 抽离能力：`should_persist_runtime_state_writes`、`build_runtime_story_state_input`
2. `TaskHandlers` 收敛为委托调用：
   - 修改 [`task_handlers.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/task_handlers.rs)
3. 模块导出更新：
   - 修改 [`mod.rs`](/F:/NovelForge/src-tauri/src/services/ai_pipeline/mod.rs)

### 已完成（Task 3：Blueprint 前端拆分并保持新逻辑兼容）

1. 抽离确定性分区工具：
   - 新增 [`certaintyZones.ts`](/F:/NovelForge/src/pages/Blueprint/utils/certaintyZones.ts)
2. 抽离确定性分区编辑器：
   - 新增 [`CertaintyZonesEditor.tsx`](/F:/NovelForge/src/pages/Blueprint/components/CertaintyZonesEditor.tsx)
3. 抽离一键编排面板：
   - 新增 [`BookPipelinePanel.tsx`](/F:/NovelForge/src/pages/Blueprint/components/BookPipelinePanel.tsx)
4. 页面收敛与旧逻辑下线：
   - 修改 [`BlueprintPage.tsx`](/F:/NovelForge/src/pages/Blueprint/BlueprintPage.tsx)
   - 删除旧内联分区工具/组件，避免新旧双路径并存

### 已完成（Task 4：Editor 前端拆分并保持新逻辑兼容）

1. 抽离 AI 流式驱动 hook：
   - 新增 [`usePipelineStream.ts`](/F:/NovelForge/src/pages/Editor/hooks/usePipelineStream.ts)
   - 收敛能力：`startPipeline`、`cancelActivePipeline`、`formatPipelineError`
   - 保持原语义：`PIPELINE_*` 错误映射、requestId 生命周期、取消语义
2. 抽离上下文侧栏组件：
   - 新增 [`EditorContextPanel.tsx`](/F:/NovelForge/src/pages/Editor/components/EditorContextPanel.tsx)
   - 承接角色/设定/剧情/名词标签页、候选资产采纳、结构化草案确认、状态摘要展示
3. 页面主文件收敛与冲突收束：
   - 修改 [`EditorPage.tsx`](/F:/NovelForge/src/pages/Editor/EditorPage.tsx)
   - 接入 `usePipelineStream` + `EditorContextPanel`
   - 删除旧内联 AI 流式驱动与旧内联侧栏实现，避免逻辑分叉
4. 契约断言补充：
   - 修改 [`editor-chapter-load-flow.test.ts`](/F:/NovelForge/tests/integration/editor-chapter-load-flow.test.ts)
   - 新增断言：`EditorPage` 必须引用 `usePipelineStream` 与 `EditorContextPanel`

### 已完成（Task 5：拆分 Settings 超大页并收束兼容桥入口）

1. 抽离模型路由面板组件：
   - 新增 [`ModelRoutingPanel.tsx`](/F:/NovelForge/src/pages/Settings/components/ModelRoutingPanel.tsx)
   - 承接“任务路由”全量 UI，与页面层状态/回调解耦
2. 抽离数据运维面板组件：
   - 新增 [`DataOpsPanel.tsx`](/F:/NovelForge/src/pages/Settings/components/DataOpsPanel.tsx)
   - 承接“数据与备份”全量 UI（备份恢复、完整性、语义诊断、Git 快照）
3. Settings 页面主文件收敛：
   - 修改 [`SettingsPage.tsx`](/F:/NovelForge/src/pages/Settings/SettingsPage.tsx)
   - 路由/数据运维区块改为组件接线，保留原有业务逻辑与状态流
4. 前端兼容桥边界收束：
   - 修改 [`pipelineApi.ts`](/F:/NovelForge/src/api/pipelineApi.ts)
   - `autoPersist` 仅作为 legacy bridge 参与推导；新增 `PIPELINE.LEGACY_POLICY_BRIDGE` 诊断日志
5. 后端兼容命令观测增强：
   - 修改 [`ai_commands.rs`](/F:/NovelForge/src-tauri/src/commands/ai_commands.rs)
   - 修改 [`settings_commands.rs`](/F:/NovelForge/src-tauri/src/commands/settings_commands.rs)
   - compatibility-only 命令除原有 deprecated 统计外，新增 `compatibility_bridge.used` 用户行为日志
6. 契约测试补充：
   - 修改 [`settings-routing-provider-guard.test.ts`](/F:/NovelForge/tests/integration/settings-routing-provider-guard.test.ts)
   - 修改 [`pipeline-persist-policy-contracts.test.ts`](/F:/NovelForge/tests/integration/pipeline-persist-policy-contracts.test.ts)
   - 新增断言：`SettingsPage` 必须接入 `ModelRoutingPanel` / `DataOpsPanel`，并保留兼容桥可观测性关键字段

## 3. 新旧逻辑冲突处理记录

1. `EditorPage` 旧内联流式驱动与新 hook 并存会造成双路径维护风险，已收敛为 hook 单一路径。
2. `EditorPage` 旧内联上下文侧栏与新组件并存会造成 UI/状态逻辑分叉，已迁移到 `EditorContextPanel`。
3. Blueprint 侧此前已完成同类收敛：旧分区工具/组件已下线，保持新逻辑唯一入口。
4. `SettingsPage` 旧内联“任务路由”巨块与新拆分组件并存会造成维护分叉，已统一收敛到 `ModelRoutingPanel`。
5. `SettingsPage` 旧内联“数据与备份”巨块与新拆分组件并存会造成变更冲突，已统一收敛到 `DataOpsPanel`。
6. `pipelineApi` 的 `autoPersist` 旧桥接继续保留，但仅限兼容推导路径并打点，避免与显式 `persistMode/automationTier` 新逻辑混淆。

## 4. 验证记录

1. 历史验证（Task 1-3 执行阶段）：
   - `cargo test --manifest-path src-tauri/Cargo.toml`：`165 passed / 0 failed`
   - `node --import tsx --test --test-isolation=none tests/integration/pipeline-persist-policy-contracts.test.ts tests/integration/blueprint-certainty-zones-contracts.test.ts`：`8 passed / 0 failed`
   - `node --import tsx --test --test-isolation=none tests/integration/pipeline-persist-policy-contracts.test.ts tests/integration/blueprint-certainty-zones-contracts.test.ts tests/integration/skill-orchestration-contracts.test.ts`：`16 passed / 0 failed`
   - `node --import tsx --test --test-isolation=none tests/integration/blueprint-certainty-zones-contracts.test.ts tests/integration/book-pipeline-orchestration.test.ts`：`18 passed / 0 failed`
2. 本次验证（Task 4）：
   - `npm run typecheck`：通过
   - `npm run build:web`：通过
   - `node --import tsx --test --test-isolation=none tests/integration/editor-chapter-load-flow.test.ts tests/state-summary-feedback.test.ts`：`5 passed / 0 failed`
3. 本次验证（Task 5）：
   - `npm run typecheck`：通过
   - `node --import tsx --test --test-isolation=none tests/integration/settings-routing-provider-guard.test.ts tests/integration/pipeline-persist-policy-contracts.test.ts`：`5 passed / 0 failed`
   - `node --import tsx --test --test-isolation=none tests/integration/settings-routing-provider-guard.test.ts tests/integration/pipeline-persist-policy-contracts.test.ts tests/integration/skill-orchestration-contracts.test.ts`：`13 passed / 0 failed`
   - `npm run build:web`：通过
   - `cargo test --manifest-path src-tauri/Cargo.toml collect_task_route_delete_ids`：`3 passed / 0 failed`

## 5. 下一步

- 按计划进入 Task 6：执行全链路验证并同步架构/运行时/API 文档。
