你是一位资深的 Rust 全栈工程师，专注于代码清理与架构修复。你的核心任务是：对 NovelForge 项目中 src-tauri 目录进行精准降噪与架构清理，所有修改必须落地到实际代码中。
当前基线状态

cargo check 结果：通过，但存在 52 条 warning
Warning 分布：

unused import：11 条
unused variable：5 条
dead_code / unused member：36 条




必须修复的问题
🔴 严重问题（必须修复）
问题1：domain 整层未接入（典型历史残留）
涉及文件：

src-tauri/src/lib.rs（第 3 行：mod domain;）
src-tauri/src/domain/chapter.rs
src-tauri/src/domain/project.rs
及 domain/ 目录下所有文件

问题表现：

crate::domain 没有任何调用方
所有枚举、结构体、转换方法均为 "never used"
架构噪声最高，严重误导后续改造方向

修复要求（必须二选一并尽快定案）：
方案操作适用场景方案A（推荐）：彻底删除删除 src-tauri/src/domain/ 目录 + lib.rs 中的 mod domain;MVP 导向，快速收敛方案B：保留并接入在真实调用链中接入 domain 模块 + 补充测试有明确长期规划必须保留
推荐方案A，理由：domain 层当前完全是死代码，继续保留只会增加维护成本和认知负担。

问题2：AI 生成链路存在旧接口壳层残留
涉及文件：

src-tauri/src/services/ai_service.rs:304, 545
src-tauri/src/services/ai_pipeline_service.rs:88

问题表现：

以下方法/结构体未被任何调用方使用：

generate_text
stream_generate
log_ai_request
complete_ai_request
AiPreviewResult
run_ai_task_pipeline（自动生成 requestId 版本）


与"AI 全链路快速生成"目标冲突
入口概念冗余，维护者难判断唯一主路径

修复要求：

识别并确认当前实际被命令层调用的方法（作为生产链路）
删除或标记为 compatibility-only：

未被任何命令层调用的方法
旧接口壳层（generate_text 等）
重复的 run_ai_task_pipeline 重载版本


设置明确的退场时间或直接移除
确保清理后 cargo check warning 减少


🟡 中等问题（必须修复）
问题3：日志机制过度设计，7 个 helper 未使用
涉及文件：

src-tauri/src/infra/logger.rs:15 ~ 74

问题表现：

以下 7 个 helper 方法均未被调用：

log_command
log_service
log_ai_call
log_db
log_fs
log_navigation
（以及 logger.rs 中其他未使用的辅助函数）



修复要求：

删除未使用的 helper，只保留当前实际被调用的：

log_startup
log_user_action
log_command_error
log_security


如有需要，保留通用 info/warn/error 基础接口
清理后验证 cargo check 无新 warning 引入


问题4：App DB 旧建表常量残留
涉及文件：

src-tauri/src/infra/app_database.rs:18

问题表现：

SCHEMA_SQL 常量已被 migration 机制替代，但仍在代码中保留
可能造成"两个 schema 真相源"并存的混淆

修复要求：

删除 SCHEMA_SQL 常量（如果 migration 机制已完全接管）
确认删除后数据库初始化仍然正常
确保 schema 唯一真相源是 migration 文件


问题5：若干服务能力定义了但未接入业务链路
涉及文件：

src-tauri/src/services/search_service.rs:24（增量索引方法）
src-tauri/src/services/chapter_service.rs:44（display_title）
src-tauri/src/infra/migrator.rs:30（MigrationResult.skipped）
src-tauri/src/services/token_budget.rs:12（TokenBudget）

问题表现：

这些能力/方法仅"存在"不"参与链路"
以"以后可能用"占位，长期来看维护成本大于收益

修复要求：

逐个评估每个未使用项：

已明确无调用场景 → 直接删除
可能有未来价值 → 标记 #[deprecated] 并加注释说明预期用途和移除条件


避免代码库中存在"占位代码"长期无人清理


🟢 建议修复（可当天完成）
问题6：机械降噪项优先清理（低风险高收益）
涉及文件：

src-tauri/src/commands/blueprint_commands.rs:4
src-tauri/src/commands/character_commands.rs:5
src-tauri/src/commands/settings_commands.rs:282
src-tauri/src/commands/skill_commands.rs:2
src-tauri/src/adapters/openai_compatible.rs:1

问题表现：

纯噪声 16 条：unused import（11 条）+ unused variable（5 条）
不影响逻辑，但影响代码可读性和编译输出

修复要求：

逐个清理 unused import 和 unused variable
清理后验证：cargo check warning 从 52 条降至约 36 条
优先处理，避免 warning 累积


修复执行约束

问题1（domain 删除）必须先确认无任何调用链，使用 grep 或代码搜索确认后再删除
问题2（AI 旧接口清理）必须先识别实际被调用的方法，避免误删生产链路
问题3-6 清理时：每次修改后执行 cargo check 验证无回归
所有删除操作：确保无其他编译单元依赖后再删除，避免破坏构建

目标指标
阶段目标当前机械降噪warning 从 52 → ~3652 条结构性清理warning 进一步降低待定最终状态cargo check 通过，warning 极低通过但有 52 条
输出要求
修复完成后，输出清理报告：
问题编号修复状态涉及文件删除/修改内容问题1✅ 已修复 / ❌ 未修复......问题2.....................
最终 cargo check warning 数量：___ 条（清理前 52 条）