你是一位资深的技术工程师，专注于 NovelForge 项目的 Prompt 体系修复与优化。你的核心任务是：严格按照审查结论，对项目中的 Prompt 相关问题执行完整修复，所有修改必须落地到实际代码中。
项目背景
NovelForge — AI 驱动的长篇小说创作辅助软件。
核心开发理念：

方便每位小说创作者，都能使用这个软件快速创作出高质量、逻辑性强的长篇小说。

核心问题背景：
当前 Prompt 体系存在多轨并存、变量注入不完整、机制空转等问题，与"AI 全链路快速生成"目标存在冲突。

审查结论摘要
已完成 Prompt 体系代码级审查，发现 6 个问题，其中：🔴 严重 2 / 🟡 中等 3 / 🟢 建议 1
核心风险：占位符注入链路不完整 与 Prompt 真相源多轨并存

必须修复的问题
🔴 严重问题（必须修复）
问题1：占位符注入链路不完整，导致关键任务输入未真正进入 Prompt
涉及文件：

src-tauri/src/services/ai_pipeline/prompt_resolver.rs:26, 27, 28
resources/builtin-skills/chapter.continue.md
resources/builtin-skills/character.create.md
resources/builtin-skills/world.create_rule.md
resources/builtin-skills/plot.create_node.md
resources/builtin-skills/consistency.scan.md
resources/builtin-skills/blueprint.generate_step.md

问题表现：

运行时只替换 {projectContext} / {userInstruction} / {selectedText}
但模板大量使用 {userDescription} / {precedingText} / {chapterContext} / {chapterContent} / {stepTitle} 等变量
造成变量悬空，部分任务用户意图实际未注入

修复要求：

建立统一渲染上下文字典，结构如下：

input — 用户输入相关变量
context — 场景上下文相关变量
task_meta — 任务元数据


在渲染后做"未解析占位符"硬校验：

发现残留即在 Prompt 阶段报错
不静默放行（当前静默放行会导致 LLM 收到未填充的占位符）


确保所有 Skill 模板中的变量都被正确注入


问题2：Prompt 真相源多轨并存，存在明显历史残留
涉及文件：

src-tauri/src/services/project_service.rs:33
src-tauri/src/services/prompt_builder.rs:47
src-tauri/src/services/ai_pipeline/prompt_resolver.rs:23
prompts/chapter-draft.json（及 prompts/*.json 全系列）

问题表现：

仓库同时存在 三套 提示词体系：

prompts/*.json
resources/builtin-skills/*.md
PromptBuilder


prompts/*.json 在运行链路中无任何读取方
但项目初始化仍创建 prompts 目录

修复要求：

收敛为单一真相源（建议二选一）：

方案A（推荐）：以 resources/builtin-skills/*.md（Skill Markdown）为唯一真相源
方案B：以结构化模板（JSON/YAML）为唯一真相源


删除未接入资产：

删除 prompts/ 目录（如果确认无调用方）
删除或废弃 PromptBuilder（如果已迁移到 Skill 模板）


移除目录初始化残留 — 检查 project_service.rs 中创建 prompts 目录的逻辑并移除


🟡 中等问题（必须修复）
问题3：promptStrategy 机制空转（配置复杂度有，运行效果无）
涉及文件：

src-tauri/src/services/skill_registry.rs:28
src-tauri/src/commands/skill_commands.rs:101
src/api/skillsApi.ts:17
src-tauri/src/services/ai_pipeline/prompt_resolver.rs:23

问题表现：

前后端都有 promptStrategy 字段（append / replace 等）
但生成链路不读取该字段
行为恒为"读全文模板后直接下发"

修复要求（二选一）：
方案操作适用场景方案A（推荐）移除该字段与 UI 展示当前无实际需求，快速收敛方案B落地策略执行器，支持 replace / append 至少两种策略有明确需求，需保留
推荐方案A，理由：机制空转是技术债务，不实现则移除。

问题4：运行时读取的是"完整 skill markdown"，包含 frontmatter 与说明文档
涉及文件：

src-tauri/src/services/skill_registry.rs:197
src-tauri/src/services/ai_pipeline/prompt_resolver.rs:23
resources/builtin-skills/chapter.draft.md:1

问题表现：

系统提示词会夹带：

YAML frontmatter
教程段落
表格示例


token 成本高且语义噪声大

修复要求：

仅渲染正文模板区块进入 LLM prompt
frontmatter 只用于元数据存储，不进入 prompt
实现"模板正文提取逻辑"：

跳过 frontmatter 区块
跳过说明文档段落
只提取实际模板内容




问题5：fallback 语义不安全——未知任务默认回落 chapter.draft
涉及文件：

src-tauri/src/services/ai_pipeline/prompt_resolver.rs:66
src-tauri/src/services/task_routing.rs:86

问题表现：

当 task 未命中 skill 模板时，直接使用章节草稿提示词
容易把非写作任务错误引导为章节生成

修复要求：

未知任务应返回明确错误码：PROMPT_TEMPLATE_NOT_FOUND
要求显式自定义模板，不应隐式降级到 chapter.draft
在 prompt_resolver.rs 中将 fallback 行为改为：

rust   // 错误写法
   Some(self.get_fallback_template()) // 隐式降级
   
   // 正确写法
   None => Err(PromptError::TemplateNotFound(task_type)) // 显式报错

🟢 建议修复
问题6：缺少 Prompt 渲染契约测试，回归风险高
涉及文件：

src-tauri/src/services/prompt_builder.rs:906
src-tauri/src/services/ai_pipeline/orchestrator.rs:178

问题表现：

现有测试覆盖 PromptBuilder
但主路径优先走 Skill 模板
缺少**"每个 canonical task 渲染后无未替换变量"**的自动化验证

修复要求：

新增 PromptResolver 集成测试矩阵：

覆盖 16 个 canonical task
每个 task 断言：

无 {...} 未解析占位符残留
必需输入字段已注入
缺参时报标准错误码（PROMPT_TEMPLATE_NOT_FOUND / MISSING_REQUIRED_INPUT）




将该测试加入 CI 流水线，防止回归


修复执行约束

问题1（变量注入）修复时：

必须确保所有 resources/builtin-skills/*.md 中的变量都被覆盖
硬校验失败必须导致任务失败，不能静默继续


问题2（真相源收敛）修复时：

必须先确认 prompts/*.json 零调用方可删除
必须先确认 PromptBuilder 无调用方方可废弃


问题4（frontmatter 分离）修复时：

修改后必须验证 LLM 收到的 prompt 不包含 frontmatter
确保 token 消耗降低


所有修复：

必须在代码注释中标注修复的问题编号
修复后执行 cargo check 确保无新 warning
涉及前端修改时执行前端构建验证