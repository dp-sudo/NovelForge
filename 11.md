P0-1: Blueprint 晋升「可见但不可达」逻辑缺陷
问题描述
Step-07-plot 配置了两个可晋升目标（剧情节点 + 叙事义务），但执行时只取 targets[0]，第二个目标不可达。

修复目标
用户看到的「可晋升资产类型：剧情节点 / 叙事义务」全部可实际晋升。

约束
不破坏现有单目标晋升逻辑
修复后通过集成测试验证多 target 全可达
保持 UI 展示与实际能力一致
验收标准
两个晋升目标均可触发并成功
每次晋升写入 entity_provenance 来源记录
UI 显示的晋升选项与实际可达一致
P0-2: 类型质量门断裂
问题描述
npm run typecheck 失败（3 处 TS 错误），但测试全部通过，掩盖了类型回归。

修复目标
修复类型错误，确保 npm run typecheck 通过；建立 CI 级别的类型门禁。

约束
测试代码类型漂移需修正
RunTaskPipelineInput.userInstruction 可选性保持
BlueprintStepData 联合类型映射需精确
验收标准
npm run typecheck 通过，无错误
测试代码类型守卫正确处理可选字段
BLUEPRINT_DEFAULTS 映射类型精确匹配
P1-1: chapter-plan 章节选择策略与 [11.md] 不一致
问题描述
文档定义了 4 种策略（user_specified / next_porous / window_drift / arc_anchor），代码仅实现 1 种。

修复目标
补齐缺失的策略分支：窗口偏差超阈值选章、弧线锚点优先。

约束
保持优先级顺序：用户指定 > 窗口下一章 > 偏差最大
窗口视野来自 AiStrategyProfile.windowPlanningHorizon
偏差计算基于 story_state 与 chapters 表
验收标准
所有 4 种策略分支可触发
窗口偏差超阈值时正确选择偏差最大章节
弧线锚点章节优先被选中
P1-2: 摘要回报口径偏差
问题描述
getSummaryFeedback 固定读取最早章节；后端按 source_chapter_id 过滤。

修复目标
改为读取「最新活跃章节」或「全局窗口 subject」聚合数据。

约束
不破坏现有上下文查询接口
返回的数据反映当前窗口最新状态
聚合逻辑在 story_state_service 层实现
验收标准
摘要回报反映当前窗口最新状态
不固定取第一章
无窗口数据时返回空，不报错
P1-3: 集成测试运行时验证不足
问题描述
测试以源码正则断言为主（readFile + assert.match），对运行时缺陷拦截能力弱。

修复目标
增加行为级测试，直接调用 API/command，断言 DB 行为和 UI 状态变化。

约束
补充多目标晋升可达性的行为测试
每个核心业务链路有运行时验证
保持现有正则测试作为补充
验收标准
多目标晋升有行为级测试覆盖
核心链路（蓝图生成 → 晋升 → 状态回写）有集成测试
测试失败能捕获真实运行时缺陷
P1-4: Rust 质量债（clippy 61 条）
问题描述
clippy -D warnings 报 61 条，无法开启严格门禁。

修复目标
分批清理技术债，建立可维护的代码质量基线。

修复优先级
批次	范围	理由
1	state.rs 构造方法	热点文件
2	task_handlers.rs 复杂度	业务核心
3	context_service.rs 复杂度	数据流关键
约束
每批配套回归测试
不改变业务逻辑
逐条修复，禁止批量 suppress
验收标准
clippy -D warnings 清零
每批修复有对应测试保护
后续新增代码不引入新 clippy 警告
P2-1: 前端包体偏大（555.40 kB）
问题描述
主包过大，触发 chunk 过大告警。

修复目标
路由级拆包，减少首屏与更新成本。

约束
Blueprint / Settings / Editor 独立打包
抽离大组件和重依赖
保持现有功能不变
验收标准
单个 chunk < 200 kB
首屏加载时间优化
路由懒加载正常
P2-2: Rust 依赖安全扫描未接入
问题描述
cargo-audit 不可用，Rust 依赖 CVE 无自动发现。

修复目标
在 CI 接入 cargo-audit，建立依赖安全门禁。

约束
设为每周/每 PR 任务
发现的漏洞按严重度分级处理
不阻断低危漏洞的正常发布
验收标准
CI 可执行 cargo-audit
高危 CVE 有告警
扫描结果可追溯