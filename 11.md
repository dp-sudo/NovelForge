# Task 1: 建立项目级 AI 策略权威源

> **冲突声明**: 如有冲突，以 `docs/superpowers/specs/2026-04-30-ai-production-system-design.md` 定义为准。

---

## 1. 任务目标

建立项目级 AI 策略的运行期权威源，以 `project.sqlite` 为唯一真相源，确保 AI 生产行为可被项目级配置控制。

### 1.1 当前问题

| 现状 | 问题 |
|------|------|
| 项目级 AI 配置缺失 | 无法按项目定制 AI 行为 |
| 无权威存储位置 | 配置散落各处，无法统一管理 |
| 与应用级配置混淆 | 项目设置与应用设置边界不清 |

### 1.2 目标状态

| 目标 | 说明 |
|------|------|
| 权威源确立 | `project.sqlite` 为项目级 AI 策略的运行期真相源 |
| 类型完整 | `AiStrategyProfile` 包含所有项目级 AI 配置字段 |
| 读写闭环 | 后端 Service + Tauri Command + 前端 API 完整链路 |
| 冷启动分离 | `project.json` 仅用于冷启动，不参与运行期 |

---

## 2. 类型体系

### 2.1 AiStrategyProfile（项目级 AI 策略）

| 字段 | 类型 | 说明 |
|------|------|------|
| `automationDefault` | `string` | 默认自动化档位（`auto` / `supervised` / `confirm`） |
| `reviewStrictness` | `number` | 审查严格度（1-5） |
| `defaultWorkflowStack` | `string[]` | 默认工作流栈（技能包 ID 列表） |
| `alwaysOnPolicySkills` | `string[]` | 常驻策略技能（术语锁定、禁用词等） |
| `defaultCapabilityBundles` | `string[]` | 默认能力包（人物表现、场景环境等） |
| `stateWritePolicy` | `string` | 状态写入策略（`chapter_confirmed` / `manual_only`） |
| `continuityPackDepth` | `string` | 上下文编译深度（`minimal` / `standard` / `deep`） |
| `chapterGenerationMode` | `string` | 章节生成模式（`draft_only` / `plan_draft` / `plan_scene_draft`） |
| `windowPlanningHorizon` | `number` | 窗口规划视野（未来章数） |

### 2.2 默认值

| 字段 | 默认值 |
|------|--------|
| `automationDefault` | `"supervised"` |
| `reviewStrictness` | `4` |
| `defaultWorkflowStack` | `["chapter.plan", "chapter.draft"]` |
| `alwaysOnPolicySkills` | `[]` |
| `defaultCapabilityBundles` | `[]` |
| `stateWritePolicy` | `"chapter_confirmed"` |
| `continuityPackDepth` | `"standard"` |
| `chapterGenerationMode` | `"plan_scene_draft"` |
| `windowPlanningHorizon` | `10` |

---

## 3. 实施边界

### 3.1 数据层（3 个文件）

| 文件 | 职责 |
|------|------|
| `src-tauri/migrations/project/0004_ai_strategy_profile.sql` | 新增 `ai_strategy_profile` TEXT 字段 |
| `src-tauri/src/infra/migrator.rs` | 注册迁移文件 |
| `src-tauri/src/infra/database.rs` | 检查迁移是否已应用 |

### 3.2 服务层（2 个文件）

| 文件 | 职责 |
|------|------|
| `src-tauri/src/services/project_service.rs` | 新增 `save_ai_strategy_profile` / `get_ai_strategy_profile` 方法 |
| `src-tauri/src/commands/project_commands.rs` | 暴露 `save_ai_strategy_profile` / `get_ai_strategy_profile` Tauri Command |

### 3.3 前端类型（2 个文件）

| 文件 | 职责 |
|------|------|
| `src/types/ai.ts` | 定义 `AiStrategyProfile` TypeScript 类型 |
| `src/api/settingsApi.ts` | 新增 `saveProjectAiStrategy` / `getProjectAiStrategy` API 函数 |

### 3.4 不做

- 不修改 `project.json` 的 AI 相关字段（保持冷启动备份角色）
- 不创建应用级 AI 策略（属于另一层级）
- 不修改任务编排器（后续任务处理）

---

## 4. 任务流程

### Step 1: 数据库迁移

**行动**: 创建 `0004_ai_strategy_profile.sql` 迁移文件，在 `projects` 表新增 `ai_strategy_profile TEXT` 字段。

**约束**: 字段允许 NULL（无配置时使用默认值）。

---

### Step 2: Rust Service 扩展

**行动**: 在 `project_service.rs` 中新增 `AiStrategyProfile` 结构体和读写方法。

**约束**:
- 使用 JSON 序列化存储
- 读取时若无配置返回默认值
- 写入时更新 `projects` 表的 `ai_strategy_profile` 字段

---

### Step 3: Tauri Command 暴露

**行动**: 在 `project_commands.rs` 中新增两个 Command，接受项目根路径和配置数据。

**约束**: Command 命名使用 snake_case，前端调用使用 camelCase。

---

### Step 4: 前端类型定义

**行动**: 在 `src/types/ai.ts` 中定义 `AiStrategyProfile` 接口。

**约束**: 字段命名与 Rust 端保持一致的 camelCase。

---

### Step 5: 前端 API 函数

**行动**: 在 `src/api/settingsApi.ts` 中新增读写函数。

**约束**: 函数命名 `saveProjectAiStrategy` / `getProjectAiStrategy`，与现有 settings API 风格一致。

---

## 5. 验收标准

1. `project.sqlite` 的 `projects` 表包含 `ai_strategy_profile` 字段
2. `save_ai_strategy_profile` Command 可将配置写入数据库
3. `get_ai_strategy_profile` Command 可从数据库读取配置
4. 无配置时返回默认值（不报错）
5. TypeScript 类型与 Rust 结构体字段一致
6. 前端 API 函数可正常调用后端 Command

---

## 6. 边界检查

| 场景 | 规则 |
|------|------|
| 项目无 AI 策略配置 | 返回默认值，不报错 |
| 配置 JSON 格式错误 | 返回错误，不静默忽略 |
| project.json 与 project.sqlite 冲突 | 以 project.sqlite 为准 |
| 类型冲突 | 以 spec 定义为准 |

---

# Task 2: 暴露项目级 AI 策略前端入口

> **冲突声明**: 如有冲突，以 `docs/superpowers/specs/2026-04-30-ai-production-system-design.md` 定义为准。

---

## 1. 任务目标

在设置页面中新增「AI 策略」标签页，作为项目级 AI 策略配置的交互入口，使用户可以查看、编辑、保存当前项目的 AI Strategy Profile。

### 1.1 当前问题

| 现状 | 问题 |
|------|------|
| AI 策略无前端入口 | 后端功能已完成，但用户无法交互 |
| 配置分散 | 项目级 AI 策略与技能系统无统一入口 |
| 面板职责不明 | 设置页面缺少专门的 AI 策略配置区 |

### 1.2 目标状态

| 目标 | 说明 |
|------|------|
| Tab 入口可见 | 设置页面显示「AI 策略」标签页 |
| 面板功能完整 | 5 个配置区（工作流、审查、状态、深度、模式） |
| 数据闭环 | 配置保存后可正确回填 |

---

## 2. 实施边界

### 2.1 新增文件

| 文件 | 职责 |
|------|------|
| `src/components/settings/AiStrategyPanel.tsx` | AI 策略配置面板主组件 |

### 2.2 修改文件

| 文件 | 改动点 |
|------|--------|
| `src/types/ai.ts` | 确认 `AiStrategyProfile` 类型定义（Task 1 已完成） |
| `src/api/settingsApi.ts` | 新增 `saveProjectAiStrategy` / `getProjectAiStrategy`（Task 1 已完成） |
| `src/pages/Settings/SettingsPage.tsx` | 在 tab 列表中新增 `aiStrategy` tab |

### 2.3 不做

- 不修改应用级 LLM 配置页面
- 不修改技能管理页面
- 不创建新的 API 路径结构
- 不拆分 `AiStrategyPanel` 为多个文件

---

## 3. 面板结构

### 3.1 配置区划分

| 配置区 | 对应字段 | 控件类型 | 说明 |
|--------|---------|---------|------|
| 默认工作流栈 | `defaultWorkflowStack` | 多选/标签输入 | 选择默认激活的技能包 |
| 审查严格度 | `reviewStrictness` | Slider (1-5) | 控制 AI 自检严格程度 |
| 状态写入策略 | `stateWritePolicy` | Select | `chapter_confirmed` / `manual_only` |
| 上下文深度 | `continuityPackDepth` | Select | `minimal` / `standard` / `deep` |
| 章节生成模式 | `chapterGenerationMode` | Select | `draft_only` / `plan_draft` / `plan_scene_draft` |
| 窗口规划视野 | `windowPlanningHorizon` | Number Input | 未来章数（1-50） |
| 常驻策略技能 | `alwaysOnPolicySkills` | 标签列表 | 已启用的策略技能 |
| 默认能力包 | `defaultCapabilityBundles` | 多选 | 已启用的能力包 |

### 3.2 UI 约束

| 约束 | 说明 |
|------|------|
| 面板标题 | "AI 策略配置" |
| Tab 图标 | 使用 `IconSettings` 或类似现有图标 |
| Tab 标签 | "AI 策略" |
| Tab 位置 | 建议在「模型」「路由」「技能」之后，「编辑器」之前 |
| 每个配置项 | 必须附带说明文字，解释该项对 AI 生产的影响 |
| 保存按钮 | 面板底部，提供保存/重置功能 |

---

## 4. 任务流程

### Step 1: 类型前置确认

**行动**: 确认 `src/types/ai.ts` 中 `AiStrategyProfile` 类型已定义完整。

**约束**: 若类型缺失，先完成类型定义再继续。

---

### Step 2: API 前置确认

**行动**: 确认 `src/api/settingsApi.ts` 中 `saveProjectAiStrategy` / `getProjectAiStrategy` 已存在。

**约束**: 若 API 缺失，先完成 API 实现再继续。

---

### Step 3: 组件构建

**行动**: 创建 `src/components/settings/AiStrategyPanel.tsx`。

**约束**:
- 使用现有 UI 组件（Card、Button、Slider、Select 等）
- 每个配置项有说明文字
- 加载状态和错误处理完整
- 默认值显示在控件上

---

### Step 4: Tab 接入

**行动**: 在 `SettingsPage.tsx` 的 tab 列表中新增 `aiStrategy`。

**约束**:
- Tab 标签：`"AI 策略"`
- 激活时渲染 `<AiStrategyPanel projectId={currentProject.id} />`
- 使用现有 UI 风格

---

### Step 5: 数据闭环验证

**行动**: 修改配置后保存，重新打开设置页面，验证数据正确回填。

**约束**: 无配置时显示默认值，不报错。

---

## 5. 验收标准

1. Settings 页面 tab 栏显示「AI 策略」入口
2. 切换到 AI 策略面板显示所有配置区
3. 修改配置后保存，数据写入 `project.sqlite` 的 `ai_strategy_profile` 字段
4. 重新打开设置页面，已保存配置正确回填
5. 无配置时显示默认值，不报错

---

## 6. 边界检查

| 场景 | 规则 |
|------|------|
| 项目无 AI 策略配置 | 显示默认值，用户可保存自定义配置 |
| 配置数据格式错误 | 显示错误提示，不崩溃 |
| 与现有 WritingStyle 面板相似 | 参考但不复用组件 |
| 类型冲突 | 以 spec 定义为准 |

---

# Task 3: 用显式持久化契约替代裸 `autoPersist`

> **冲突声明**: 如有冲突，以 `docs/superpowers/specs/2026-04-30-ai-production-system-design.md` 定义为准。

---

## 1. 任务目标

将所有 AI 任务入口中的裸 `autoPersist: true` 替换为显式声明的 `persistMode` 和 `automationTier`，建立清晰的三档自动化控制与三类持久化语义。

### 1.1 当前问题

| 现状 | 问题 |
|------|------|
| `autoPersist: true` | 语义模糊，无法区分写入正式库还是审阅层 |
| 无自动化档位 | 无法区分自动执行还是需用户确认 |
| 风险等级隐式 | 无法快速识别高风险操作 |

### 1.2 目标状态

| 目标 | 收益 |
|------|------|
| 显式声明持久化模式 | 入口语义一目了然 |
| 显式声明自动化档位 | 风险等级可评估 |
| 保留兼容桥 | 不破坏现有调用链 |

---

## 2. 类型体系

### 2.1 PersistMode（持久化模式）

定义三种持久化语义：

| 值 | 语义 | 典型场景 |
|---|------|---------|
| `formal` | 晋升为正式资产，写入 Canon Registry | 蓝图生成、资产种子 |
| `derived_review` | 派生审阅层，记录但不写正式库 | 时间线审阅、关系图审阅 |
| `none` | 仅生成，不持久化 | 草稿预览、一致性检查 |

### 2.2 AutomationTier（自动化档位）

定义三档自动化控制：

| 值 | 语义 | 适用条件 |
|---|------|---------|
| `auto` | 自动执行，用户无感知 | 低风险操作 |
| `supervised` | 执行后通知用户审查 | 正式资产写入 |
| `confirm` | 执行前需用户确认 | 高影响操作 |

---

## 3. 实施边界

### 3.1 核心类型扩展（3 个文件）

| 层级 | 文件 | 扩展点 |
|------|------|--------|
| 前端基础 | `src/api/pipelineApi.ts` | `RunTaskPipelineInput` 新增 `persistMode`、`automationTier` |
| 前端模块 | `src/api/moduleAiApi.ts` | `RunModuleAiTaskInput` 新增相同字段 |
| 后端服务 | `src-tauri/.../ai_pipeline_service.rs` | `RunAiTaskPipelineInput` 新增 `persist_mode`、`automation_tier` |

### 3.2 入口显式声明（11 个文件）

| 文件 | 入口场景 | persistMode | automationTier |
|------|---------|-------------|----------------|
| `blueprintApi.ts` | 蓝图建议生成 | `formal` | `supervised` |
| `bookPipelineApi.ts` | 蓝图阶段（8 个） | `formal` | `supervised` |
| `bookPipelineApi.ts` | 资产种子阶段（5 个） | `formal` | `confirm` |
| `bookPipelineApi.ts` | 章节规划 | `formal` | `auto` |
| `characterApi.ts` | AI 生成角色 | `formal` | `supervised` |
| `worldApi.ts` | AI 生成世界规则 | `formal` | `supervised` |
| `glossaryApi.ts` | AI 生成术语 | `formal` | `supervised` |
| `plotApi.ts` | AI 生成剧情节点 | `formal` | `supervised` |
| `narrativeApi.ts` | AI 生成叙事义务 | `formal` | `confirm` |
| `TimelinePage.tsx` | AI 时间线审阅 | `derived_review` | `auto` |
| `RelationshipsPage.tsx` | AI 关系审阅 | `derived_review` | `auto` |

### 3.3 不做

- 不删除 `autoPersist` 字段，保留兼容桥
- 不修改任务编排器内部逻辑
- 不创建新的 API 路径结构
- 不修改手动创建入口（`createCharacter` 等）

---

## 4. 任务流程

### Step 1: 前端基础类型扩展

**行动**: 在 `pipelineApi.ts` 中定义 `PersistMode` 和 `AutomationTier` 类型，并在 `RunTaskPipelineInput` 中扩展字段。

**约束**: 类型必须导出，供其他模块导入使用。

---

### Step 2: 前端模块级 API 扩展

**行动**: 在 `moduleAiApi.ts` 中同步新增两个字段到 `RunModuleAiTaskInput`。

**约束**: 字段命名与 `pipelineApi.ts` 保持一致。

---

### Step 3: Rust 后端类型扩展

**行动**: 在 `ai_pipeline_service.rs` 的 `RunAiTaskPipelineInput` 中新增对应字段。

**约束**: 字段命名使用 snake_case（Rust 惯例），但 JSON 序列化保持 camelCase。

---

### Step 4: 逐入口显式声明

**行动**: 按实施边界表格逐个修改各文件的 AI 调用，添加 `persistMode` 和 `automationTier` 字段。

**约束**: 不删除现有的 `autoPersist: true`，保留兼容。

---

### Step 5: 兼容桥说明

**行动**: 在 `pipelineApi.ts` 中添加注释说明兼容桥规则。

**规则**: 当只传 `autoPersist: true` 且未传 `persistMode` 时，按以下推断：
- `taskType` 包含 `review` → `persistMode = "derived_review"`
- `taskType` 包含 `blueprint` → `persistMode = "formal"`
- 其他 → 默认 `persistMode = "formal"`
- `automationTier` 默认 `supervised`

---

## 5. 验收标准

1. `RunTaskPipelineInput` 包含 `persistMode` 和 `automationTier` 字段
2. 所有蓝图相关入口携带 `persistMode: "formal", automationTier: "supervised"`
3. 所有资产种子入口携带 `persistMode: "formal", automationTier: "supervised"`
4. 叙事义务入口携带 `persistMode: "formal", automationTier: "confirm"`
5. 所有审阅层入口携带 `persistMode: "derived_review", automationTier: "auto"`
6. 现有 `autoPersist: true` 调用不报错

---

## 6. 边界检查

| 场景 | 规则 |
|------|------|
| `autoPersist` 与 `persistMode` 同时存在 | 以 `persistMode` 为准 |
| 新建 AI 入口未声明 | 参照任务类型推断（见兼容桥规则） |
| 类型冲突 | 以 spec 定义为准 |

---

# Task 4: 收口全书生成与模块晋升边界，并记录来源轨迹

> **冲突声明**: 如有冲突，以 `docs/superpowers/specs/2026-04-30-ai-production-system-design.md` 定义为准。

---

## 1. 任务目标

将全书生成流程拆分为「蓝图生成」与「资产晋升」两个独立阶段，并为每个正式资产的创建记录来源轨迹（provenance），建立从草案到权威的可追溯链路。

### 1.1 当前问题

| 现状 | 问题 |
|------|------|
| 全书生成跨层执行 | 蓝图生成与资产晋升混在同一流程中 |
| 无来源记录 | 无法追溯正式资产来自哪个 AI 输出 |
| 边界模糊 | 蓝图页同时负责生成和晋升 |
| 审阅层无提示 | 用户无法区分「正式数据」和「派生审阅」 |

### 1.2 目标状态

| 目标 | 说明 |
|------|------|
| 阶段分离 | 全书生成 = 蓝图阶段（8步）+ 晋升阶段（独立操作） |
| 来源可追溯 | 每个正式资产记录其生成来源（蓝图草案 / AI 输出 / 用户输入） |
| 边界收口 | 蓝图页只负责蓝图，资产晋升显式触发 |
| 审阅提示 | 派生审阅层页面显示「本页为审阅层」提示 |

---

## 2. 类型体系

### 2.1 阶段类型

| 阶段 | 语义 | persistMode | automationTier |
|------|------|-------------|----------------|
| `blueprint-first` | 蓝图生成阶段（8步） | `formal` | `supervised` |
| `promote-second` | 资产晋升阶段（独立触发） | `formal` | `confirm` |

### 2.2 来源类型（Provenance Source Kind）

| 值 | 语义 | 典型场景 |
|---|------|---------|
| `blueprint_draft` | 蓝图草案生成 | 蓝图各步骤的 AI 输出 |
| `ai_generation` | AI 直接生成 | 角色/世界/剧情种子 |
| `user_input` | 用户手动创建 | 手动录入的角色 |
| `manual_promotion` | 手动晋升 | 用户确认后晋升 |
| `auto_promotion` | 自动晋升 | supervised 档位自动晋升 |

### 2.3 来源记录字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `entity_type` | `string` | 实体类型（`character` / `world_rule` / `plot_node` 等） |
| `entity_id` | `string` | 实体 ID |
| `source_kind` | `string` | 来源类型（见上表） |
| `source_ref` | `string?` | 来源引用（如蓝图 stepKey / 请求 ID） |
| `request_id` | `string?` | AI 请求 ID |
| `created_at` | `string` | 创建时间 |

---

## 3. 实施边界

### 3.1 数据层（2 个文件）

| 文件 | 职责 |
|------|------|
| `src-tauri/migrations/project/0005_entity_provenance.sql` | 新建 `entity_provenance` 表 |
| `src-tauri/src/infra/migrator.rs` | 注册迁移文件 |

### 3.2 服务层（3 个文件）

| 文件 | 职责 |
|------|------|
| `src-tauri/src/services/ai_pipeline/task_handlers.rs` | 资产晋升时写入来源记录 |
| `src-tauri/src/services/ai_pipeline_service.rs` | 支持蓝图与晋升两阶段分离 |
| `src-tauri/src/services/context_service.rs` | 查询实体时携带来源信息 |

### 3.3 前端（2 个文件）

| 文件 | 职责 |
|------|------|
| `src/api/bookPipelineApi.ts` | 拆分为 `buildBookStages` 和 `buildPromotionStages` |
| `src/pages/Blueprint/BlueprintPage.tsx` | 新增「确认并晋升」按钮 |

### 3.4 不做

- 不修改 Skill 系统
- 不修改 State Ledger（Task 5 处理）
- 不修改 Continuity Pack（Task 6 处理）
- 不修改审阅层页面内部逻辑

---

## 4. 任务流程

### Step 1: 数据库迁移

**行动**: 创建 `0005_entity_provenance.sql` 迁移文件，新建 `entity_provenance` 表。

**约束**:
- 表字段：`id`, `project_id`, `entity_type`, `entity_id`, `source_kind`, `source_ref`, `request_id`, `created_at`
- 索引：`(project_id, entity_type, entity_id)`

---

### Step 2: 阶段分离

**行动**: 将 `bookPipelineApi.ts` 的 `buildBookStages` 拆分为两个函数。

**约束**:
- `buildBookStages`: 返回蓝图阶段（8步）
- `buildPromotionStages`: 返回晋升阶段（角色/世界/剧情/术语/义务/章节计划）
- 两阶段独立调用，不自动串接

---

### Step 3: 来源记录写入

**行动**: 在 `task_handlers.rs` 的资产晋升处理中，新增 `record_entity_provenance` 调用。

**约束**:
- 晋升成功时写入来源记录
- `source_kind` 根据晋升触发方式确定
- `entity_id` 使用新创建的实体 ID

---

### Step 4: 蓝图页晋升按钮

**行动**: 在 `BlueprintPage.tsx` 的蓝图步骤完成区，新增「确认并晋升」按钮。

**约束**:
- 按钮位于步骤完成后的操作区
- 触发时调用晋升阶段的第一个步骤
- 显示当前可晋升的资产类型

---

### Step 5: 审阅层提示

**行动**: 在 `TimelinePage.tsx` 和 `RelationshipsPage.tsx` 添加审阅层提示。

**约束**:
- 在页面顶部或信息区添加提示文字
- 提示内容：「本页为派生审阅层，展示已晋升的正式数据与待审信息」
- 使用次要样式，不干扰主流程

---

## 5. 验收标准

1. `entity_provenance` 表已创建，包含所有必要字段
2. 全书生成流程拆分为蓝图阶段（8步）+ 晋升阶段（独立）
3. 角色/世界/剧情/术语/义务晋升时写入来源记录
4. 蓝图页显示「确认并晋升」按钮
5. 时间线页和关系图页显示审阅层提示
6. 来源信息可通过 context_service 查询

---

## 6. 边界检查

| 场景 | 规则 |
|------|------|
| 手动创建实体无 AI 来源 | `source_kind = "user_input"` |
| 自动晋升与手动晋升 | 自动晋升 `source_kind = "auto_promotion"`，手动晋升 `source_kind = "manual_promotion"` |
| 蓝图阶段与晋升阶段顺序 | 蓝图阶段可独立完成，晋升阶段可延迟 |
| 类型冲突 | 以 spec 定义为准 |

---

# Task 5: 建立 State Ledger 并接入章节写后回写

> **冲突声明**: 如有冲突，以 `docs/superpowers/specs/2026-04-30-ai-production-system-design.md` 定义为准。

---

## 1. 任务目标

建立 State Ledger（状态账本），以结构化方式记录章节写后的实体状态变化，并为上下文编译提供增量状态摘要。

### 1.1 当前问题

| 现状 | 问题 |
|------|------|
| 无状态记录层 | 章节写后实体状态变化无法追踪 |
| 上下文缺失状态 | Continuity Pack 无法注入最新状态 |
| 状态散落各处 | 状态信息分散在正文和备注中 |
| 无增量更新 | 每次生成都重新计算全部状态 |

### 1.2 目标状态

| 目标 | 说明 |
|------|------|
| 状态账本建立 | `story_state` 表记录所有实体状态变化 |
| 写后回写接入 | 章节保存后自动触发状态抽取与写入 |
| 上下文集成 | 状态摘要可注入 Continuity Pack |
| 增量更新 | 只记录增量变化，不重复存储 |

---

## 2. 类型体系

### 2.1 状态主体类型

| subject_type | 说明 | 典型 state_kind |
|--------------|------|----------------|
| `character` | 角色实体 | `emotion` / `location` / `relation` / `belief` |
| `world_element` | 世界元素 | `status` / `availability` / `condition` |
| `plot` | 剧情节点 | `progress` / `resolution` / `conflict_level` |
| `relationship` | 关系状态 | `warmth` / `trust` / `tension` |

### 2.2 状态范围

| scope | 语义 | 说明 |
|-------|------|------|
| `chapter` | 单章级状态 | 本章内的临时状态变化 |
| `arc` | 弧线级状态 | 贯穿多章的角色/剧情弧 |
| `global` | 全局状态 | 贯穿全书的重要状态 |

### 2.3 story_state 表字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `TEXT` | 状态记录 ID |
| `project_id` | `TEXT` | 项目 ID |
| `subject_type` | `TEXT` | 主体类型 |
| `subject_id` | `TEXT` | 主体 ID |
| `scope` | `TEXT` | 状态范围 |
| `state_kind` | `TEXT` | 状态种类 |
| `payload_json` | `TEXT` | 状态载荷（JSON） |
| `source_chapter_id` | `TEXT?` | 来源章节 ID |
| `status` | `TEXT` | 状态（`active` / `superseded`） |
| `created_at` | `TEXT` | 创建时间 |
| `updated_at` | `TEXT` | 更新时间 |

---

## 3. 实施边界

### 3.1 数据层（2 个文件）

| 文件 | 职责 |
|------|------|
| `src-tauri/migrations/project/0006_story_state.sql` | 新建 `story_state` 表 |
| `src-tauri/src/infra/migrator.rs` | 注册迁移文件 |

### 3.2 服务层（3 个文件）

| 文件 | 职责 |
|------|------|
| `src-tauri/src/services/story_state_service.rs` | 新建状态账本服务 |
| `src-tauri/src/services/context_service.rs` | 状态摘要查询 |
| `src-tauri/src/services/chapter_service.rs` | 章节保存后触发状态回写 |

### 3.3 前端（2 个文件）

| 文件 | 职责 |
|------|------|
| `src/api/contextApi.ts` | 新增状态摘要查询接口 |
| `src/pages/Editor/EditorPage.tsx` | 章节保存后触发状态更新 |

### 3.4 不做

- 不修改技能系统（Task 7-8 处理）
- 不修改 Continuity Pack（Task 6 处理）
- 不修改晋升逻辑（Task 4 已完成）
- 不修改蓝图生成逻辑

---

## 4. 任务流程

### Step 1: 数据库迁移

**行动**: 创建 `0006_story_state.sql` 迁移文件，新建 `story_state` 表。

**约束**:
- 字段：见类型体系 2.3
- 索引：`(project_id, subject_type, subject_id, state_kind, status)`

---

### Step 2: 服务实现

**行动**: 创建 `story_state_service.rs`，实现状态存取方法。

**约束**:
- `upsert_state`: 写入或更新状态
- `list_latest_states`: 查询某主体的最新状态
- `list_chapter_states`: 查询某章节相关的所有状态

---

### Step 3: 章节保存接入

**行动**: 在 `chapter_service.rs` 的 `save_chapter_content` 成功后，调用 `story_state_service` 记录窗口进度。

**约束**:
- 章节保存成功后才触发
- 记录当前章节索引和字数
- 作为全局窗口状态

---

### Step 4: 上下文接口扩展

**行动**: 在 `contextApi.ts` 新增 `getChapterContext` 接口，返回章节上下文包含状态摘要。

**约束**:
- 返回 `ChapterContext` 包含 `stateSummary` 数组
- 每个状态摘要包含 `subjectType`、`subjectId`、`stateKind`、`payload`

---

### Step 5: 编辑器集成

**行动**: 在 `EditorPage.tsx` 章节保存后，调用 `getChapterContext` 更新上下文状态显示。

**约束**:
- 保存成功后刷新上下文
- 状态变化时显示简要提示

---

## 5. 验收标准

1. `story_state` 表已创建，包含所有必要字段
2. `StoryStateService` 实现 `upsert_state` 和 `list_latest_states` 方法
3. 章节保存后自动触发状态回写
4. `getChapterContext` 接口返回包含 `stateSummary` 的上下文
5. 编辑器页面在保存后正确刷新上下文状态
6. 状态查询支持按主体类型和 ID 过滤

---

## 6. 边界检查

| 场景 | 规则 |
|------|------|
| 同一主体同一状态Kind多次写入 | 更新 `updated_at`，旧记录标记 `status = "superseded"` |
| 查询无状态的主体 | 返回空数组，不报错 |
| 状态载荷 JSON 格式错误 | 写入时验证，不接受非法 JSON |
| 类型冲突 | 以 spec 定义为准 |

---

# Task 6: 实现 Continuity Pack 编译器并接入 Prompt 解析

> **冲突声明**: 如有冲突，以 `docs/superpowers/specs/2026-04-30-ai-production-system-design.md` 定义为准。

---

## 1. 任务目标

实现 Continuity Pack 编译器，将 Constitution（蓝图）、Canon（资产）、State（状态）、Promise（承诺）、Window（窗口）五类上下文编译为结构化包，注入 Prompt 解析器，实现写前上下文标准化。

### 1.1 当前问题

| 现状 | 问题 |
|------|------|
| 上下文拼接散乱 | 各类型上下文以不同方式注入 Prompt |
| 无统一编译层 | Prompt 解析器直接读取数据库拼接 |
| 状态上下文缺失 | State Ledger 未接入上下文编译 |
| 锁定术语未注入 | `locked_terms` / `banned_terms` 未进入 Prompt |

### 1.2 目标状态

| 目标 | 说明 |
|------|------|
| 编译器统一 | `ContinuityPackCompiler` 统一编译五类上下文 |
| 结构化输出 | 编译结果为 `ContinuityPack` 结构，包含 7 类上下文 |
| 解析器消费 | `PromptResolver` 消费 `ContinuityPack` 而非直接拼接 |
| 术语注入 | 锁定术语和禁用词纳入 Lexicon Policy Context |

---

## 2. 类型体系

### 2.1 ContinuityPack 结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `constitution_context` | `string[]` | 总书/卷级方向约束 |
| `canon_context` | `string[]` | 角色/世界/名词/剧情正式资产 |
| `lexicon_policy_context` | `string[]` | 锁定术语/禁用词/文风约束 |
| `state_context` | `string[]` | 当前实体状态摘要 |
| `promise_context` | `string[]` | 未兑现的叙事义务 |
| `window_plan_context` | `string[]` | 章节窗口计划 |
| `recent_continuity_context` | `string[]` | 近期章节承接 |

### 2.2 上下文编译深度

| depth | 含义 | 使用场景 |
|-------|------|---------|
| `minimal` | 只含 Lexicon Policy | 快速预览、草稿生成 |
| `standard` | 含前 5 类 | 常规章节生成 |
| `deep` | 含全部 7 类 | 重要章节、高风险场景 |

---

## 3. 实施边界

### 3.1 新建文件（1 个）

| 文件 | 职责 |
|------|------|
| `src-tauri/src/services/ai_pipeline/continuity_pack.rs` | 编译器核心实现 |

### 3.2 修改文件（4 个）

| 文件 | 改动点 |
|------|--------|
| `src-tauri/src/services/context_service.rs` | 提供编译器所需上下文数据 |
| `src-tauri/src/services/ai_pipeline/prompt_resolver.rs` | 消费 `ContinuityPack` 而非直接拼接 |
| `src-tauri/src/services/ai_pipeline/orchestrator.rs` | 调用编译器生成包 |
| `src-tauri/src/services/chapter_service.rs` | 提供窗口计划数据 |

### 3.3 不做

- 不修改 State Ledger（Task 5 已完成）
- 不修改技能系统（Task 7-8 处理）
- 不修改晋升逻辑（Task 4 已完成）
- 不修改前端 UI（纯后端改造）

---

## 4. 任务流程

### Step 1: 编译器骨架

**行动**: 创建 `continuity_pack.rs`，定义 `ContinuityPack` 结构体和 `ContinuityPackCompiler` 及其方法。

**约束**:
- `compile(project_root, canonical_task, depth) -> ContinuityPack`
- 支持 `minimal` / `standard` / `deep` 三种深度
- 方法按需懒加载，避免不必要的数据库查询

---

### Step 2: 上下文数据源接入

**行动**: 修改 `context_service.rs`，为编译器提供查询接口。

**约束**:
- `get_constitution_context()`: 返回蓝图相关上下文
- `get_canon_context()`: 返回正式资产上下文
- `get_state_summary()`: 返回状态账本摘要
- `get_promise_context()`: 返回未兑现义务
- `get_window_plan()`: 返回章节窗口计划
- `get_recent_continuity()`: 返回近期章节承接

---

### Step 3: Lexicon Policy 编译

**行动**: 在编译器中实现术语锁定和禁用词注入逻辑。

**约束**:
- 从 `glossary_terms` 表读取 `locked = true` 的术语
- 从 `glossary_terms` 表读取 `banned = true` 的术语
- 输出格式：`"锁定术语: xxx、xxx"` / `"禁用词: xxx、xxx"`

---

### Step 4: PromptResolver 改造

**行动**: 修改 `prompt_resolver.rs`，消费 `ContinuityPack` 而非直接拼接字符串。

**约束**:
- 调用 `ContinuityPackCompiler::compile` 生成包
- 按节注入各类上下文到 Prompt
- 深度由 `AiStrategyProfile.continuityPackDepth` 决定

---

### Step 5: Orchestrator 集成

**行动**: 在 `orchestrator.rs` 的 Prompt 构建阶段调用编译器。

**约束**:
- 在 `PHASE_PROMPT` 阶段之前调用
- 传递当前任务的 `canonical_task` 和项目根路径
- 将生成的包传递给 `PromptResolver`

---

## 5. 验收标准

1. `ContinuityPack` 包含全部 7 类上下文字段
2. `ContinuityPackCompiler::compile` 支持三种深度
3. 锁定术语和禁用词正确注入 `lexicon_policy_context`
4. `PromptResolver` 消费 `ContinuityPack` 而非直接拼接
5. 不同 `continuityPackDepth` 配置产生不同的上下文注入量
6. 编译器错误不影响主流程（返回空上下文而非崩溃）

---

## 6. 边界检查

| 场景 | 规则 |
|------|------|
| 上下文数据为空 | 该类上下文返回空数组 |
| 编译器错误 | 记录日志，返回空 `ContinuityPack` |
| 配置深度不存在 | 默认使用 `standard` |
| 类型冲突 | 以 spec 定义为准 |